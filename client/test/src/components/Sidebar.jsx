import React, { useState } from "react";

const Sidebar = () =>{
    return(
        <div>
            {/* Gotta find a way to ensure the sidebar renders different content based on the page it renders in */}
        </div>
    );
}

export default Sidebar;